---
layout: post
title: New release for the 0.6 alpha development
h1: New release for the 0.6 alpha development
kind: article
menu: blog
created_at: 2013-10-15
location: Albi, France
author: Théo de la Hogue
categories:
- release
- 0.6
---

p=. <%= fancyImage @item, "images/0.6.0-alpha-way-of-patching.png", "images/0.6.0-alpha-way-of-patching.png", "0.6 way of patching" %>

The Jamoma 0.6 alpha version development continu. The new way of patching is closed to be finalized and many fixes have been made in "real life" situation to ensure Max patchers stability.
The 0.6 version provides a clear separation between the *model* (where the core execution happens) and its *views* (where gui complexity are handled) and now the *module* simply eases the creation of a standard view for a model in one shot.

However if the model/view separation convention is boring you, it is now possible to do Max and then add Jamoma at any level of your patchers. Any where you'll add a *j.parameter | j.message | j.return* you'll still keep the benefits of the many externals the 0.6 version provides like : 

- *j.cue* to store and recall the state of your patcher,
- *j.namespace* to explore all the parameters registered and manage selection for cue management,
- *j.map* to enable a mapping between two remotes j.parameter | j.message | j.return and automatically scale the value to the appropriate range bounds and dataspace unit,
- *j.send(~)* and *j.receive(~)* which are particularly powerful considering the use of wildcard in their address to bind to a set of j.parameter | j.message | j.return.

Finally the 0.6 version will provides an original *protocol solution* to easily set, get, listen and discover any registered model, view, parameter, message or return your patcher from a remote network client (unfortunatelly this feature cannot be considered as completed in the 0.6.0-a25 version but we encourage you to look at the *j.modular.maxhelp* to know more about this experimental development).

If you're insterested in testing the new 0.6.0-a25 release you can get it "here"=http://www.jamoma.org/download/0.6/.


